# Email acceptance test

Use an isolated account and prove verification, password reset, invitation, warning, hard-budget, trial-ending, payment-failed and subscription-active templates. Confirm one delivery record per idempotency key, recipient hashes only, retry after a temporary failure, no enumeration difference, and no token in application logs. Production remains NO-GO until the verified domain receives every required message.
