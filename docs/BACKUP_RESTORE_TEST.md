# Backup and restore test

Confirm the Supabase plan's backup and point-in-time recovery capabilities before launch. Quarterly, restore into an isolated project, run migrations, reconstruct counters with the guarded reconciliation RPC in dry-run then repair mode, verify tenant counts and totals, and destroy the isolated restore after evidence is retained. Never test destructive restore against production.
